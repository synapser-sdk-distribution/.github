# .github

## What is the Entry System?

The Entry System is Synapser's passwordless biometric authentication approach. Instead of relying on reusable passwords, it verifies a real, live user and uses single-use encryption keys for each authentication event to help reduce account takeover risk while keeping login simple.

Learn more in the official Entry SDK documentation: https://synapser-limited.github.io/entry-sdk-docs/
